setInterval(fun,time):
每隔一段时间执行一次规定的函数，一直循环下去，时间以毫秒为单位。
clearInterval(timer):
清除时间函数，终止时间函数继续执行。

步骤：
1.添加页面元素，实现页面布局
2.在页面布局的基础上，通过使用JavaScript来控制操作按钮，从而实现页面的交互效果。

JavaScript代码写在全文的下面，并且放在window.onload里面来执行：
   是为了当JS去获取页面中的图片时，也能获取图片的实际宽高，因为图片的加载可能在JS之后。
只用图片的宽度值来判断图片等比缩放


